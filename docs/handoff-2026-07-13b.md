# Handoff pro další autonomní běh — 2026-07-13 (večerní stav)

Navazuje na `docs/handoff-2026-07-13.md`. Stav: main = prod, repo čisté, 421 testů zelených,
tsc baseline 22, deploye #86–#88 úspěšné, prod health db:ok.

## Nepřekročitelný invariant majitele (beze změny)

> „hlavně ať je tam ta lidská kontrola u těch položek které jsou v nabídce."

Per-item attestace z JWT, generate bez potvrzených cen padá (400), žádné bulk/auto potvrzování.
Money-path změny → Codex adversarial review před mergem, nálezy verify-first.

## Co přibylo v tomto běhu (PR #86–#88, vše nasazeno)

- **C-01b** (PR #86) — inbox bulk generate/finalize: per-zakázka gate (`findUnconfirmedPrices`
  + přepočet HARD flagů, 409 s důvodem, nikdy all-or-nothing, bulk nikdy nepotvrzuje ceny —
  regression test); governance per iteraci; finalize cap 20; řazení lhůta × skóre.
  Adversarial review REJECT→fix (governance bypass, fail-open legacy single-product gate, UI důvody).
- **C-02** (PR #87) — cost widget: `kc_na_cn` (CN = kanonický `cenova_nabidka.docx`),
  14denní trend v Přehledu.
- **C-03b** (PR #88) — Slack warn při 80 % stropu (1×/den, atomický claim), stav
  `budget_paused` na hranici kroku, resume vždy znovu přes money-gate.
  **DAN-TODO: nastavit `VZ_WATCHDOG_SLACK_WEBHOOK_URL` v prod env** (bez něj alert nechodí,
  pauza funguje).
- **B-02 audit** — implementace kompletní (dvoufázový řetěz, ekvivalent nikdy automatický,
  UI badge, cost log). Akceptace hit-rate ≥ 70 % NEPOTVRZENÁ: offline eval 13. 7. dává
  verify hit-rate 56,25 % při pokrytí 4,88 % — čeká na živá verify data z pilotů.

## Známé nálezy z review, které NEjsou opravené (vědomě)

- Legacy `potvrzeno: true` bez JWT auditní stopy prochází generate money-gate (pre-existující
  migrační rozhodnutí C-01; submit gate: all-legacy = warning, mixed = problem). Pokud má
  být zpřísněno, je to samostatné rozhodnutí Dana — zpřísnění by zablokovalo staré zakázky.

## Fronta na další běh

1. ~~C-04~~ — HOTOVO dodatečně v témže dni (PR #90, nasazeno). Zároveň proveden živý smoke
   C-01b na prod: bulk generate i finalize vrací `started:[]` + per-zakázka 409 s důvodem
   a seznamem položek; nic se nespustilo, potvrzení nedotčena.
2. **C-05 / C-06** dle plánu (zkontrolovat detail v `docs/plan/03-implementacni-plan.md`).
3. **B-02 měření** — po prvních živých verify bězích přeměřit hit-rate (eval report `output/eval/`).
4. **D-vlna** — STÁLE NEZAČÍNAT (až po prvním reálném výsledku).

## Jak jet (ověřený postup — beze změny)

Viz `docs/handoff-2026-07-13.md` sekce „Jak jet" (Codex gpt-5.6-sol implementace, zadání do
souboru, task --write z kořene, sekvenčně, gates sám, merge z kořene, deploy → nginx reload →
health → _migrations). Migrace: další volné číslo **023** (tento běh žádnou nepřidal).

## Co blokuje byznys (Dan, ne kód)

Pilot MO lhůta **16. 7. 9:00**: 4 kvalifikační doklady (0/4) + potvrdit 14 položek
(runbook `docs/plan/04-runbook-podani.md`). Zakázka `porizeni-monitorovaciho-sitoveho-nastroje`
na prod NEMAZAT (důkaz E2E, čeká na potvrzení cen). Nově: prod env
`VZ_WATCHDOG_SLACK_WEBHOOK_URL` pro 80% alert.
