# Handoff pro další autonomní běh — 2026-07-13

Pro příští session (Fable orchestrace, **implementace Codex gpt-5.6-sol**). Stav je čistý:
main = prod, žádná rozdělaná větev, origin uklizený, 405 testů zelených.

## Nepřekročitelný invariant majitele

> „hlavně ať je tam ta lidská kontrola u těch položek které jsou v nabídce."

Každá cena v nabídce = potvrzení člověkem (per-item attestace, serverová auditní stopa z JWT).
Generate bez potvrzených cen tvrdě padá (živě ověřeno HTTP 400). Nikdy nezavádět bulk/auto
potvrzení, ani „dočasně". Oponentura každé money-path změny povinná (Codex adversarial review).

## Co je nasazeno (poslední vlna, PR #78–#84)

- **A-01** úplnost balíku vs. ZD — HARD gate; potvrzení nese sha256+fingerprint; vymyšlený požadavek lze auditovaně zamítnout
- **A-04** outcome watcher — **NEN publikuje výsledky veřejně** (`/detail-zakazky/<id>/vysledek`); watcher zakládá jen NÁVRHY (migrace 021), potvrzuje člověk
- **B-06** fill miss-rate — nevyplněný povinný slot blokuje podání; fill-report.json
- **A-03** feature vektory skóre (migrace 022, append-only, best-effort) — kalibrace má vstupy
- **B-05** auto-run-all po převzetí — zastaví se na `waiting_approval`; governance `ai_jobs=false` převzetí nerozbije
- Živý E2E 13. 7.: převzetí → ZD → analýza → nacenění → waiting_approval **bez kliknutí**; zakázka `porizeni-monitorovaciho-sitoveho-nastroje` na prod ČEKÁ na potvrzení cen (4 položky) — nemazat, je to důkaz

## Fronta na další běh (z docs/plan/03-implementacni-plan.md)

1. **C-01b** — inbox jako pracovní plocha: bulk generate/finalize JEN nad zakázkami se 100 % attestovaných položek (jinak per-zakázka 409 s důvodem, nikdy tichý průchod); řazení lhůta × skóre. Pozor na bug-vzor „desync draftu" (PR #53) — regresní test povinný.
2. **C-02 zbytek** — `GET /api/costs/summary` UŽ EXISTUJE (ověřeno na prod); zbývá ověřit/doplnit widget v PrehledPage (spend dnes/měsíc, Kč/CN, trend) a agregace den/týden/měsíc.
3. **C-03b** — strop AI: včasné varování (80 %) + šetrná pauza běžící práce místo tvrdého 503.
4. **B-02 audit** — verify fallback řetěz je z velké části hotový (two-phase + cache + backfill); projít task v plánu, doimplementovat jen chybějící, odškrtnout.
5. **D-vlna** (price-to-win, kalibrace) — AŽ po prvním reálném výsledku; bez dat je to věštění.

## Jak jet (ověřený postup)

- Skill **codex-delegace-orchestrace** načíst PŘED prvním Codex krokem.
- Vzor: zadání do souboru ve scratchpadu → `node ~/.claude/plugins/cache/openai-codex/codex/*/scripts/codex-companion.mjs task --write "$(cat zadani.md)"` z KOŘENE repa, hlavní smyčka + `run_in_background`, sekvenčně (nikdy 2 runy naráz nad repem).
- Zadání vždy: větev už připravená (`git checkout -B feature/x` PŘED runem), ŽÁDNÉ git příkazy pro Codex, output contract JSON, verifikace v zadání.
- Po runu gates ověřit SÁM: `cd scripts && npm test` (405+, 0 fail), `npx tsc --noEmit` (baseline ~22 chyb, 0 nových), `cd apps/web && npm run build`.
- Commit/PR/merge z KOŘENE repa (`git add scripts apps config` — cwd po buildech bývá apps/web → „pathspec 'scripts' did not match").
- Deploy: merge do main → GitHub Actions → po doběhu `docker exec makemore-nginx nginx -s reload` (stale IP) → `curl https://vz.ludone.cz/api/health` → ověřit poslední migraci v `_migrations`.
- Migrace: další volné číslo **023**.
- Money-path změny: Codex adversarial review PŘED mergem (read-only `task`, verdikt + nálezy; verify-first — každý nález ověřit v kódu).

## Co blokuje byznys (úkoly majitele, ne kódu)

Pilot **Nákup drobného nářadí** (MO, lhůta **16. 7. 9:00**): (a) 4 kvalifikační doklady
(Nastavení → Firmy, 0/4: výpis OR, rejstřík trestů, FÚ, OSSZ), (b) projít 14 položek a potvrdit
ceny (runbook `docs/plan/04-runbook-podani.md`). Dále A-00 (entita, NEN registrace, právní
konzultace). **První reálné podání > další feature.**
