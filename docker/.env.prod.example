# Required
ANTHROPIC_API_KEY=sk-ant-...
API_TOKEN=your-secret-api-token
JWT_SECRET=<64-char hex from: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))">

# Optional
AI_MODEL=claude-sonnet-4-6
API_PORT=3001

# Caddy domain (used in Caddyfile)
DOMAIN=vz.yourdomain.com
