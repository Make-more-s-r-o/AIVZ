# VZ AI Tool — Production environment
# Copy to .env.prod and fill in values

# Required: AI API key
ANTHROPIC_API_KEY=sk-ant-...

# Required: API auth token (generate a random string)
API_TOKEN=

# Optional: AI model override
AI_MODEL=claude-sonnet-4-5-20250929

# Docker image (set by CI/CD, usually don't change)
VZ_IMAGE=ghcr.io/make-more-s-r-o/aivz:latest

# Port mapping (default 3001, change if conflicts)
VZ_PORT=3001

# Caddy reverse proxy (only used with --profile with-caddy)
DOMAIN=vz.srv997068.hstgr.cloud
CADDY_HTTP_PORT=8080
CADDY_HTTPS_PORT=8443
